Report a structured result back to the parent/orchestrator session. Call this before your final message when the parent asked you to produce specific output — a verdict, summary, counters, or any free-form JSON object up to 4KB serialized. The result is persisted on your session row and delivered to the parent in the task-completion frame. A root session (no parent) receives a no-op warning instead.
